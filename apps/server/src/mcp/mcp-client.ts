// Optional MCP client wiring goes here.
// Expose MCP tools to the agent, e.g., web browser, eBay, internal systems.
export function attachMcpServers(_rt: any) {
  // rt.registerTool("ebay.createListing", async (args) => {...})
}
